// THE LICENSED WORK IS PROVIDED UNDER THE TERMS OF THE ADAPTIVE PUBLIC LICENSE
// ("LICENSE") AS FIRST COMPLETED BY: Guillermo Adrián Molina. ANY USE, PUBLIC
// DISPLAY, PUBLIC PERFORMANCE, REPRODUCTION OR DISTRIBUTION OF, OR PREPARATION OF
// DERIVATIVE WORKS BASED ON, THE LICENSED WORK CONSTITUTES RECIPIENT'S ACCEPTANCE
// OF THIS LICENSE AND ITS TERMS, WHETHER OR NOT SUCH RECIPIENT READS THE TERMS OF
// THE LICENSE. "LICENSED WORK" AND "RECIPIENT" ARE DEFINED IN THE LICENSE. A COPY
// OF THE LICENSE IS LOCATED IN THE TEXT FILE ENTITLED "LICENSE.TXT" ACCOMPANYING
// THE CONTENTS OF THIS FILE. IF A COPY OF THE LICENSE DOES NOT ACCOMPANY THIS
// FILE, A COPY OF THE LICENSE MAY ALSO BE OBTAINED AT THE FOLLOWING WEB SITE:
// https://github.com/guillermomolina/protos
//
// Software distributed under the License is distributed on an "AS IS" basis,
// WITHOUT WARRANTY OF ANY KIND, either express or implied. See the License for
// the specific language governing rights and limitations under the License.

call: (...elements) => {
    result: IdentityMap()
    elements.each((element) => {
        result.atPut(element, true)
    })
    result
}

contains: (set, element) => {
    set.containsKey(element)
}

size: (set) => {
    set.size()
}

add: (set, element) => {
    set.atPut(element, true)
    set
}

remove: (set, element) => {
    set.remove(element)
    set
}

each: (set, block) => {
    set.each((element, marker) => {
        block(element)
    })
    set
}

union: (left, right) => {
    result: IdentityMap()
    left.each((element, marker) => {
        result.atPut(element, true)
    })
    right.each((element, marker) => {
        result.atPut(element, true)
    })
    result
}

intersection: (left, right) => {
    result: IdentityMap()
    left.each((element, marker) => {
        right.containsKey(element).ifTrue(() => {
            result.atPut(element, true)
        })
    })
    result
}

difference: (left, right) => {
    result: IdentityMap()
    left.each((element, marker) => {
        right.containsKey(element).ifFalse(() => {
            result.atPut(element, true)
        })
    })
    result
}

sameMembers: (left, right) => {
    (left.size() == right.size()).ifFalse(() => {
        ^false
    })
    left.each((element, marker) => {
        right.containsKey(element).ifFalse(() => {
            ^false
        })
    })
    true
}

isSubset: (left, right) => {
    left.each((element, marker) => {
        right.containsKey(element).ifFalse(() => {
            ^false
        })
    })
    true
}

isSuperset: (left, right) => {
    right.each((element, marker) => {
        left.containsKey(element).ifFalse(() => {
            ^false
        })
    })
    true
}

isDisjoint: (left, right) => {
    left.each((element, marker) => {
        right.containsKey(element).ifTrue(() => {
            ^false
        })
    })
    true
}

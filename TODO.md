
If code changes update version (minor or patch) in pom.xml, update CHANGELOG.md with code changes.
If spec changes update version (minor or patch) in files at /docs, update PROTOS_SPEC_CHANGELOG.md with spec changes.
Do git commit (include manually changed files). do not push.


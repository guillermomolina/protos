# Filesystem

Status: Migration index — non-normative

The normative material for this domain currently remains in `../PROTOS_IO_MODEL.md`.
This file exists to establish the target modular specification layout. It must not be used as an independent source of observable semantics until the corresponding migration revision explicitly transfers normative ownership here.
